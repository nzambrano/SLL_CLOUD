apply plugin: "war"

bootWar {
    mainClassName = "com.prosegur.sllpe.SllpeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
